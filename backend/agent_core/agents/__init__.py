"""Reasoning units. `task_agent` (Text→Text) is implemented; `language_agent`
(language detection/confidence scoring) is a later phase — SessionState's
language fields are set by the caller directly until then."""
